/**
 * VTuber Fire Head Tracker
 * 
 * A 2D VTuber-style application that tracks the user's head via webcam
 * and renders a fire particle effect that follows head movement.
 * 
 * Uses OpenCV DNN for face detection and Raylib for rendering.
 */

#include "App.h"

int main()
{
    App app;
    app.Run();
    return 0;
}
